# Server configuration
SERVER_PORT=3000
# Path to SQLite file; for production set an absolute path. For tests use :memory:
SQLITE_FILE=./prayers.db
# Frontend dev hint (set this in your frontend .env to point to this API)
# VITE_API_URL=http://localhost:3000/api
#
# MongoDB (preferred) - do NOT commit credentials. Example (set in your env):
# MONGO_URL=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/mydb
