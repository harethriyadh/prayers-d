{
  "name": "prayers-server",
  "version": "0.1.0",
  "description": "Backend API for prayers SPA (Express + SQLite)",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon --watch src --exec node src/index.js",
    "start": "node src/index.js",
    "test": "cross-env NODE_ENV=test jest --runInBand --detectOpenHandles",
    "lint": "echo \"No lint configured\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^5.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.6.1",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3",
    "mongodb-memory-server": "^8.13.1"
  },
  "scripts": {
    "dev": "nodemon --watch src --exec node src/index.js",
    "start": "node src/index.js",
    "test": "cross-env NODE_ENV=test jest --runInBand --detectOpenHandles",
    "lint": "echo \"No lint configured\"",
    "seed": "node ./scripts/seed.js"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true
  }
}
